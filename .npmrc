# NPM Configuration
# Prevents various npm issues and optimizes installation

# Reduce npm logging noise
loglevel=warn

# Save dependencies to package.json by default
save=true

# Use standard audit fix behavior
audit=true

# Use legacy peer deps if needed
legacy-peer-deps=false

# Optimize package installation
depth=0
